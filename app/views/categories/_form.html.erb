<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field register">
    <%= form.label :Nombre_de_la_categoria %>
    <%= form.text_field :name, class: "form-control form-control-sm" %> <br>
  </div>

  <div class="actions">
    <%= form.submit "Agregar", :class => "btn btn-success col-md-12" %>
  </div>
  <br>
  <div class="actions">
    <%= link_to 'Volver', admins_path, class: "btn btn-warning col-md-12" %>
  </div>
  <br><br>
<% end %>
