<nav class="navbar navbar-expand-md navbar-light bg-light sticky-top">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <ul class=" navbar-nav justify-content">
    <% if user_signed_in? %>
      <% if current_user.avatar.attached? %>
    <li class = "showuser btn">  Hola, <%= current_user.name %> <%= image_tag current_user.avatar.variant(resize: "50x50!"), class: "rounded-circle" %></li>
      <% else %>
        <%= image_tag gravatar_image_url(current_user.email, size: 50), class: "rounded-circle" %>
      <% end %>
    <% end %>
  </ul>

  <div class="navbar-collapse collapse w-100 order-3 dual-collapse2" id="navbarSupportedContent">
    <ul class=" navbar-nav ml-auto">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <li class="btn"><%= link_to image_tag('hom2.png', :size => "40x40"), admins_path%></li>
        <% else %>
          <li class="btn"><%= link_to image_tag('hom2.png', :size => "40x40"), root_path%></li>
          <li class="btn"><%= link_to image_tag('carro.png', :size => "45x45"), orders_path%></li>
        <% end %>
      <% else %>
        <li class = "btn">
          <a class="nav-link" href="<%= root_path %>">Home <span class="sr-only">(current)</span></a>
        </li>
      <% end %>
      <% if user_signed_in? %>
        <li class = "btn"><%= link_to 'Mi Cuenta', edit_user_registration_path, class: "btn btn-warning col-12" %></li>
        <li class = "btn"><%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class: "btn btn-danger col-12" %></li>
      <%else%>
        <li class = "btn"><%= link_to 'Iniciar Sesión', new_user_session_path, class: "btn btn-success col-12" %></li>
        <li class = "btn"><%= link_to 'Registrarse', new_user_registration_path, class: "btn btn-warning col-12" %></li>
      <%end%>
    </ul>
  </div>
</nav>
