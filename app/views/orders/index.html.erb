<div class="container">
  <div class="row">
    <div class="col-md-3">

    </div>
    <div class="col-md-6">
      <table class="table">
        <% @orders.each do |order| %>
            <tr>
              <td>  <%= order.product.name %> </td>
              <td>$ <%= order.product.price %> </td>
              <td><%= link_to image_tag('deletebtn.jpg', :size => "50x50"), order, method: :delete, data: {confirm: "Estás seguro?"}%></td>
            </tr>
        <% end %>
        <tr>
          <td> Total: </td>
          <td>$ <%= @orders.map{|order| order.product.price }.sum %></td>
        </tr>
      </table>

      <%= link_to 'Volver', products_path, class: "btn btn-outline-secondary col-md-12" %>
      </div>
    <div class="col-md-3">

    </div>
  </div>
</div>
